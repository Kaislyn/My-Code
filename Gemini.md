### 第一题：和为 S 的组合

**题目描述：**
给定两个整数 $N$ 和 $S$。请你找出从 $1$ 到 $N$ 中，选出**任意个数**（至少选 $1$ 个），使得它们的**和恰好等于 $S$** 的所有组合。
按组合内数字从小到大输出，每个组合占一行。

**输入格式：**
两个空格隔开的整数 $N$ 和 $S$ ($1 \le N \le 20$, $1 \le S \le 50$)。

**输出格式：**
满足条件的组合，每行一个。

**样例输入：**
```text
5 7
```

**样例输出：**
```text
1 2 4
2 5
3 4
```

---

### 第二题：迷宫路径条数

**题目描述：**
给定一个 $N$ 行 $M$ 列的二维迷宫。`0` 表示可以走的路，`1` 表示墙壁（障碍物）。
你现在站在左上角 `(1, 1)`，想要走到右下角 `(N, M)`。每次只能向**上、下、左、右**走一格，且不能重复经过同一个格子。
请问一共有多少条不同的路径可以到达终点？

**输入格式：**
第一行两个整数 $N$, $M$ ($1 \le N, M \le 6$)。
接下来 $N$ 行，每行 $M$ 个整数（`0` 或 `1`），代表迷宫地图。

**输出格式：**
一个整数，表示总路径条数。如果走不到，输出 `0`。

**样例输入：**
```text
3 3
0 0 0
0 1 0
0 0 0
```

**样例输出：**
```text
2
```